const initialState = {
  cost: "RM 12000.00",
  budget: "RM 34000.00",
  costCategory: "EEIU",
  costCentre: "BMCE02"
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
