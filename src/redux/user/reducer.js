const initialState = {
  name: 'Ali',
  staffId: 'T626616',
  division: 'GDC',
  authenticated: 'true'
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
