const initialState = {
  requestorName: "Abu bin Abhadaf",
  requestorDivision: "webe",
  additionalTravellerName1: "ascvasv bin aava",
  additionalTravellerDivision1: "webe"
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
